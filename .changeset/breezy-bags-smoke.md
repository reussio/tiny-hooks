---
"tiny-hooks": minor
---

Add a new hook `useLocalStorage` to the library. This hook provides a typed API to read, write, and remove values from localStorage, with optional onError handling and automatic synchronization across browser tabs.
