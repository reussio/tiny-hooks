---
"tiny-hooks": minor
---

Add a new hook `useIsMounted` to the Library. This hybrid hook provides both a constant `isMounted` for render-time checks and a function `getIsMounted` for async-safe mounted state detection.
