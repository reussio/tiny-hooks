---
"tiny-hooks": minor
---

Add a new hook `useIsClient` to the Library. This hook provides a simple way to detect if the code is running in the browser (client-side) or during server-side rendering.
